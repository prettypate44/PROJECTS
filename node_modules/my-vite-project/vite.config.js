import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [vue()],
  build: {
    lib: {
      entry: './src/index.js', // Path to the entry point of your library
      name: 'MyViteProject', // Global variable name for UMD build
      fileName: (format) => `my-vite-project.${format}.js`,
    },
    rollupOptions: {
      // Externalize dependencies to reduce bundle size
      external: ['vue'],
      output: {
        globals: {
          vue: 'Vue', // Make Vue available globally for UMD builds
        },
      },
    },
  },
});
